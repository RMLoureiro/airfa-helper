# ── Database ───────────────────────────────────────────────────────────────────
# Use Dokploy UI: Databases → Create → PostgreSQL, then copy the connection string here.
# Format: postgresql://USER:PASSWORD@HOST:5432/DB_NAME
DATABASE_URL=postgresql://airfa:change-me@localhost:5432/airfa

# ── Backend ────────────────────────────────────────────────────────────────────
# Generate a strong secret: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-generate-a-random-64-char-hex-string

# ── CORS ───────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins (no trailing slash)
CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ── Seed ───────────────────────────────────────────────────────────────────────
# Set RUN_SEED=true on FIRST deploy only to create the super admin account.
# Set it back to false before restarting to avoid re-running the seed.
RUN_SEED=false

# Super admin account credentials (applied when RUN_SEED=true)
SUPER_ADMIN_USERNAME=admin
SUPER_ADMIN_NAME=Administrador Principal
SEED_SUPER_ADMIN_PASSWORD=change-me-strong-admin-password

# ── Domains (Dokploy / Traefik) ────────────────────────────────────────────────
# Subdomain that points to the backend API (used in Traefik router rule)
API_DOMAIN=api.your-domain.com
# Root domain / subdomain that points to the frontend
APP_DOMAIN=your-domain.com

# ── Frontend ───────────────────────────────────────────────────────────────────
# Public URL of the backend API as seen from the user's browser.
# NOTE: This is baked in at Docker build time (Next.js NEXT_PUBLIC_* variable).
#       Rebuild the frontend image if this value changes.
NEXT_PUBLIC_API_URL=https://api.your-domain.com
